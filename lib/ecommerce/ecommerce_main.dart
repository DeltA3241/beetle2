import 'package:flutter/material.dart';

class ECommerceMain extends StatelessWidget {
  const ECommerceMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Ecommerce');
  }
}
